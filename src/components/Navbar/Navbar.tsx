import React from "react";
import Button from "../../ui/Button";

const Navbar: React.FC = () => {
  return (
    <div>
      {/* KODE PANJANG MEMBUAT NAVBAR */} <Button />
    </div>
  );
};

export default Navbar;
